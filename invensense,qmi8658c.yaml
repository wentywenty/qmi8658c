description: QST QMI8658C 6-axis IMU

compatible: "invensense,qmi8658c"

include: [sensor-device.yaml, i2c-device.yaml]

properties:
  int-gpios:
    type: phandle-array
    description: |
      GPIO connected to the sensor's INT1 pin.
      This is used for data-ready interrupt signal.
